from setuptools import setup, find_packages

setup(
    name="puflin",
    version="0.1",
    author="rightrev",
    author_email="engg@rightrev.com",
    description="RightRev GraphQL service to query Snowflake warehouse.",
    packages=[],
    package_dir={'puflin': '.'},
    install_requires=[
                      ]
)
